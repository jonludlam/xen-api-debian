--- a/scripts/vif
+++ b/scripts/vif
@@ -255,7 +255,7 @@
     fi
     ;;
 
-remove)
+remove | offline)
     if [ "${TYPE}" = "vif" ] ;then
         call_hook_script $DOMID "${ACTION}"
 		# Unclear whether this is necessary, since netback also does it:
